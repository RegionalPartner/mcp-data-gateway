# TEMPLATE ONLY — never commit with real values
# Apply manually: kubectl create secret generic mcp-gateway-secrets --from-literal=... -n mcp-demo
# See docs/OVH_DEPLOYMENT.md Part 7.1 for the full kubectl command.
apiVersion: v1
kind: Secret
metadata:
  name: mcp-gateway-secrets
  namespace: mcp-demo
type: Opaque
stringData:
  db-url: "REPLACE_WITH_JDBC_URL"
  db-user: "REPLACE_WITH_DB_USER"
  db-password: "REPLACE_WITH_DB_PASSWORD"
  mcp-hmac-pepper: "REPLACE_WITH_MCP_HMAC_PEPPER"   # openssl rand -hex 32
  mcp-content-key: "REPLACE_WITH_MCP_CONTENT_KEY"   # openssl rand -hex 32 (exactly 64 hex chars)
