apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  namespace: mcp-demo
spec:
  selector:
    app: mcp-gateway
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
